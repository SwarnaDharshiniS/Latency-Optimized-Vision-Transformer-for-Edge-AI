# EdgeViT: Latency-Optimized Vision Transformer for Edge AI
# Install: pip install -r requirements.txt
# Trimmed to packages actually imported in the notebook (see cell 52 for the
# exact pinned versions captured from the training run).

torch>=2.2.0
torchvision>=0.17.0

numpy>=1.24
matplotlib>=3.8
seaborn>=0.13

scikit-learn>=1.4
scikit-image>=0.22

Pillow>=10.0

onnx>=1.16
onnxruntime>=1.17
onnxscript>=0.1.0

gradio>=4.20

jupyter>=1.0
ipykernel>=6.29
